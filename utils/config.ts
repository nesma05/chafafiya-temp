const config = {
  glApi: process.env.NEXT_PUBLIC_GOOGLE_API_KEY,
  glContextKey: process.env.NEXT_PUBLIC_GOOGLE_CONTEXT_KEY,
  baseUrl:process.env.NEXT_PUBLIC_BASE_URL,
  envMode:process.env.NODE_ENV
}

export default config
